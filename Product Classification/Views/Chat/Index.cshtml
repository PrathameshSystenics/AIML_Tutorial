@{
    ViewData["Title"] = "Chat";
}

<div class="container-fluid" style="min-height: calc(100vh - 120px);">
    <div class="d-flex flex-column">

        <!-- Model Selection Area -->
        <div class="p-3">
            <div class="d-flex align-items-center">
                <label for="modelSelect" class="me-2 fw-bold">Select Model:</label>
                <select id="modelSelect" name="modelSelect" class="form-select" style="max-width: 200px;">
                    <option>GPT-3.5</option>
                    <option>GPT-4</option>
                </select>
            </div>
        </div>

        <!-- Chat Messages Area (scrollable) -->
        <div class="flex-grow-1 overflow-auto p-3" style="height:70vh;">
            <!-- AI Message (Left Side) -->
            <div class="d-flex mb-3">
                <div class="bg-success text-white rounded p-2 d-flex align-items-center justify-content-center me-2" style="width: 40px; height: 40px; min-width: 40px;">
                    AI
                </div>
                <div class="bg-white rounded p-3 shadow-sm" style="max-width: 80%;">
                    <div class="fw-bold mb-1">ChatGPT</div>
                    <p class="mb-2">To create a responsive website, you can:</p>
                    <ol>
                        <li>Use a responsive framework like Bootstrap</li>
                        <li>Implement media queries in your CSS</li>
                        <li>Use flexible grid layouts</li>
                        <li>Design with a mobile-first approach</li>
                    </ol>
                    <p class="mb-0">Would you like me to elaborate on any of these points?</p>
                </div>
            </div>

            <!-- User Message (Right Side) -->
            <div class="d-flex mb-3 justify-content-end">
                <div class="bg-light rounded p-3 shadow-sm" style="max-width: 80%;">
                    <div class="fw-bold mb-1">You</div>
                    <p class="mb-0">Yes, please explain more about media queries and how they work.</p>
                </div>
                <div class="bg-primary text-white rounded p-2 d-flex align-items-center justify-content-center ms-2" style="width: 40px; height: 40px; min-width: 40px;">
                    U
                </div>
            </div>

            <!-- Another AI Message (Left Side) -->
            <div class="d-flex mb-3">
                <div class="bg-success text-white rounded p-2 d-flex align-items-center justify-content-center me-2" style="width: 40px; height: 40px; min-width: 40px;">
                    AI
                </div>
                <div class="bg-white rounded p-3 shadow-sm" style="max-width: 80%;">
                    <div class="fw-bold mb-1">ChatGPT</div>
                    <p class="mb-2">Media queries are a CSS technique that allows you to apply different styles based on device characteristics, primarily the screen width. Here's how they work:</p>
                    <p class="mb-0">This conditional CSS only applies when the screen width is 768px or less, allowing you to create tailored layouts for various devices.</p>
                </div>
            </div>

            <!-- Another AI Message (Left Side) -->
            <div class="d-flex mb-3">
                <div class="bg-success text-white rounded p-2 d-flex align-items-center justify-content-center me-2" style="width: 40px; height: 40px; min-width: 40px;">
                    AI
                </div>
                <div class="bg-white rounded p-3 shadow-sm" style="max-width: 80%;">
                    <div class="fw-bold mb-1">ChatGPT</div>
                    <p class="mb-2">Media queries are a CSS technique that allows you to apply different styles based on device characteristics, primarily the screen width. Here's how they work:</p>
                    <p class="mb-0">This conditional CSS only applies when the screen width is 768px or less, allowing you to create tailored layouts for various devices.</p>
                </div>
            </div>

            <!-- Another AI Message (Left Side) -->
            <div class="d-flex mb-3">
                <div class="bg-success text-white rounded p-2 d-flex align-items-center justify-content-center me-2" style="width: 40px; height: 40px; min-width: 40px;">
                    AI
                </div>
                <div class="bg-white rounded p-3 shadow-sm" style="max-width: 80%;">
                    <div class="fw-bold mb-1">ChatGPT</div>
                    <p class="mb-2">Media queries are a CSS technique that allows you to apply different styles based on device characteristics, primarily the screen width. Here's how they work:</p>
                    <p class="mb-0">This conditional CSS only applies when the screen width is 768px or less, allowing you to create tailored layouts for various devices.</p>
                </div>
            </div>

            <!-- Another AI Message (Left Side) -->
            <div class="d-flex mb-3">
                <div class="bg-success text-white rounded p-2 d-flex align-items-center justify-content-center me-2" style="width: 40px; height: 40px; min-width: 40px;">
                    AI
                </div>
                <div class="bg-white rounded p-3 shadow-sm" style="max-width: 80%;">
                    <div class="fw-bold mb-1">ChatGPT</div>
                    <p class="mb-2">Media queries are a CSS technique that allows you to apply different styles based on device characteristics, primarily the screen width. Here's how they work:</p>
                    <p class="mb-0">This conditional CSS only applies when the screen width is 768px or less, allowing you to create tailored layouts for various devices.</p>
                </div>
            </div>
        </div>

        <div class="p-3 bg-white border-top">
            <div class="position-relative">
                <textarea class="form-control chat-textarea" placeholder="Enter Your Message..." rows="2" style="resize: none; padding-right: 50px;"></textarea>
                <button class="btn btn-primary rounded-circle position-absolute" id="chat-button" style="bottom: 8px; right: 8px;" type="button">
                    <i class="bi bi-send-fill"></i>
                </button>
            </div>
            <div class="text-center mt-2">
                <small class="text-muted">
                    <i class="bi bi-info-circle"></i>
                    Product Bot can make mistakes. Consider checking important information.
                </small>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    <script src="~/js/Chat.js"></script>
}